name: Deploy to Oracle

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: [self-hosted, oracle]
    steps:
      - name: Deploy
        run: |
          cd /opt/paperreader
          git fetch origin main
          git reset --hard origin/main
          npm ci
          npm run build
          sudo systemctl restart paperreader